# Build config for HTTP runtime (default: urania sandbox)

INCLUDE=endpoints.md responses.md patterns.md

# Template variables — injected into SKILL.md at build time
BASE_URL_VAR=DATA_PROXY_BASE
AUTH_NOTE=Auth is automatic (proxy injects credentials). Never hardcode keys.
